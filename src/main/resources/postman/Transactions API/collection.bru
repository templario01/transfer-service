meta {
  name: Transactions API
}

auth {
  mode: none
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

docs {
  REST API collection for handling account transfers including optional FX conversion.
}
